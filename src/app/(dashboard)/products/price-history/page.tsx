'use client';
import {useEffect,useState} from 'react';
export default function PriceHistory(){const[id,setId]=useState('');const[name,setName]=useState('');const[d,setD]=useState<any[]>([]);const[e,setE]=useState('');async function load(){setE('');const j=await(await fetch(`/api/price-history?productId=${id}`)).json();if(!j.success){setE(j.error||'Failed');return}setD(j.data||[])}return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">Price History</h1><p className="text-sm text-slate-500">Historical product prices never replace the values stored on old orders.</p></div><div className="flex max-w-xl gap-2"><input value={id} onChange={x=>setId(x.target.value)} placeholder="Product ID" className="w-40 rounded-xl border p-3"/><button onClick={load} className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-medium text-white">Load history</button></div>{e&&<div className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{e}</div>}<div className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">{name||'Product price timeline'}</h2><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-3">Valid From</th><th className="p-3 text-right">Purchase</th><th className="p-3 text-right">Sale</th><th className="p-3 text-right">Margin</th><th className="p-3">Currency</th><th className="p-3">Created By</th></tr></thead><tbody className="divide-y">{d.map(x=><tr key={x.id}><td className="p-3">{new Date(x.valid_from).toLocaleString()}</td><td className="p-3 text-right">{Number(x.purchase_price||0).toFixed(2)}</td><td className="p-3 text-right font-medium">{Number(x.sale_price||0).toFixed(2)}</td><td className="p-3 text-right">{x.margin_percent==null?'—':`${Number(x.margin_percent).toFixed(2)}%`}</td><td className="p-3">{x.currency}</td><td className="p-3">{x.created_by_name||'—'}</td></tr>)}</tbody></table>{!d.length&&<p className="p-8 text-center text-sm text-slate-500">No price history found.</p>}</div></div></div>}