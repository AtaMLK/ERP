'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Login failed');router.replace('/');router.refresh();}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="min-h-screen grid lg:grid-cols-2 bg-slate-50"><section className="hidden lg:flex bg-slate-950 text-white p-12 flex-col justify-between"><div><div className="text-2xl font-bold tracking-tight">FZ<span className="text-indigo-400">ERP</span></div><p className="mt-3 text-slate-400 max-w-md">A secure operations workspace for orders, purchasing, finance and logistics.</p></div><p className="text-sm text-slate-500">FZ ERP • Business Management</p></section><section className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm space-y-6"><div><div className="lg:hidden text-2xl font-bold mb-10">FZ<span className="text-indigo-600">ERP</span></div><h1 className="text-3xl font-semibold tracking-tight text-slate-950">Welcome back</h1><p className="text-slate-500 mt-2">Sign in to your workspace.</p></div>{error&&<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<label className="block text-sm font-medium">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 outline-none focus:ring-2 focus:ring-indigo-500" /></label><label className="block text-sm font-medium">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3 outline-none focus:ring-2 focus:ring-indigo-500" /></label><button disabled={busy} className="w-full rounded-xl bg-slate-950 py-3 font-medium text-white hover:bg-slate-800 disabled:opacity-50">{busy?'Signing in…':'Sign in'}</button></form></section></main>}
