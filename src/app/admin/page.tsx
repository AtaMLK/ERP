'use client';
import {useEffect,useState} from 'react';
export default function Admin(){const[data,setData]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/admin/users').then(r=>r.json()).then(j=>setData(j.data||[])).finally(()=>setLoading(false))},[]);return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Admin</h1><p className="text-sm text-slate-500">Users, roles and individual permission overrides</p></div><div className="rounded-2xl border bg-white p-4"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">ERP Users</h2><span className="rounded-full bg-slate-100 px-3 py-1 text-xs">{data.filter(x=>!x.deleted_at).length} / 5 active</span></div>{loading?<p>Loading…</p>:<div className="overflow-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50"><tr><th className="p-3 text-left">Name</th><th className="p-3 text-left">Email</th><th className="p-3 text-left">Role</th><th className="p-3 text-left">Status</th><th className="p-3 text-left">Overrides</th></tr></thead><tbody>{data.map(u=><tr className="border-t" key={u.id}><td className="p-3 font-medium">{u.name}</td><td className="p-3">{u.email}</td><td className="p-3">{u.role}</td><td className="p-3">{u.deleted_at?'Inactive':'Active'}</td><td className="p-3">{u.overrides?.length||0}</td></tr>)}</tbody></table></div>}</div></div>}
