import {NextRequest,NextResponse} from 'next/server'; import AuthService from '@/lib/services/AuthService'; import {pool} from '@/lib/db';
export async function POST(req:NextRequest){try{const body=await req.json();if(typeof body.email!=='string'||typeof body.password!=='string')return NextResponse.json({success:false,error:'Email and password are required'},{status:400});const {user,token}=await new AuthService(pool).login(body.email,body.password);const res=NextResponse.json({success:true,data:{user}});res.cookies.set('fz_session',token,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:8*60*60});return res}catch(e:any){return NextResponse.json({success:false,error:e.message||'Invalid credentials'},{status:e.message==='Invalid credentials'?401:500})}}
