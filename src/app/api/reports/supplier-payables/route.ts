import{NextRequest}from'next/server';import{pool}from'@/lib/db';import{getSession,requirePermission,handleApiError}from'@/lib/api/guards';export const dynamic='force-dynamic';export async function GET(req:NextRequest){try{const u=await getSession(req);requirePermission(u,'reports:read');requirePermission(u,'financial_data:read');const r=await pool.query(`SELECT si.id,si.invoice_number,si.invoice_date,si.due_date,si.currency,s.name supplier,si.amount total_amount,COALESCE(SUM(sp.amount),0)::numeric paid_amount,(si.amount-COALESCE(SUM(sp.amount),0))::numeric outstanding,GREATEST(CURRENT_DATE-COALESCE(si.due_date::date,CURRENT_DATE),0)::int days_overdue,CASE WHEN COALESCE(si.due_date::date,CURRENT_DATE)>=CURRENT_DATE THEN 'current' WHEN CURRENT_DATE-si.due_date::date BETWEEN 1 AND 30 THEN '1-30' WHEN CURRENT_DATE-si.due_date::date BETWEEN 31 AND 60 THEN '31-60' WHEN CURRENT_DATE-si.due_date::date BETWEEN 61 AND 90 THEN '61-90' ELSE '90+' END aging_bucket FROM supplier_invoices si JOIN suppliers s ON s.id=si.supplier_id LEFT JOIN supplier_payments sp ON sp.supplier_invoice_id=si.id WHERE si.status NOT IN('PAID','CANCELLED','paid','cancelled') GROUP BY si.id,s.name HAVING si.amount-COALESCE(SUM(sp.amount),0)>0 ORDER BY days_overdue DESC,si.due_date ASC`);return Response.json({success:true,data:r.rows})}catch(e){return handleApiError(e)}}
