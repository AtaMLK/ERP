import {NextRequest,NextResponse} from 'next/server';import jwt from 'jsonwebtoken';
export function middleware(req:NextRequest){if(req.nextUrl.pathname.startsWith('/api/auth'))return NextResponse.next();if(req.nextUrl.pathname.startsWith('/api/')||req.nextUrl.pathname!=='/login'){const token=req.cookies.get('fz_session')?.value;const secret=process.env.JWT_SECRET;if(!token||!secret){if(req.nextUrl.pathname.startsWith('/api/'))return NextResponse.json({success:false,error:'Authentication required'},{status:401});return NextResponse.redirect(new URL('/login',req.url))}try{jwt.verify(token,secret)}catch{if(req.nextUrl.pathname.startsWith('/api/'))return NextResponse.json({success:false,error:'Invalid session'},{status:401});return NextResponse.redirect(new URL('/login',req.url))}}return NextResponse.next()}
export const config={matcher:['/((?!_next/static|_next/image|favicon.ico).*)']};
