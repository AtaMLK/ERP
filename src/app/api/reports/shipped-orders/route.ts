import{NextRequest}from'next/server';import{pool}from'@/lib/db';import{getSession,requirePermission,handleApiError}from'@/lib/api/guards';export const dynamic='force-dynamic';export async function GET(req:NextRequest){try{const u=await getSession(req);requirePermission(u,'reports:read');const s=req.nextUrl.searchParams,from=s.get('from')||'2000-01-01',to=s.get('to')||'2999-12-31';const r=await pool.query(`SELECT sh.id,sh.shipment_number,sh.shipment_date,o.order_number,c.name customer,COALESCE(SUM(shi.quantity),0)::numeric quantity_shipped,COALESCE(SUM(shi.quantity*oi.unit_sale_price),0)::numeric shipment_value,sh.status,sh.incoterm_code FROM shipments sh JOIN orders o ON o.id=sh.order_id JOIN customers c ON c.id=o.customer_id JOIN shipment_items shi ON shi.shipment_id=sh.id JOIN order_items oi ON oi.id=shi.order_item_id WHERE sh.shipment_date>= $1::date AND sh.shipment_date < ($2::date+INTERVAL '1 day') GROUP BY sh.id,sh.shipment_number,sh.shipment_date,o.order_number,c.name,sh.status,sh.incoterm_code ORDER BY sh.shipment_date DESC`,[from,to]);return Response.json({success:true,data:r.rows})}catch(e){return handleApiError(e)}}
